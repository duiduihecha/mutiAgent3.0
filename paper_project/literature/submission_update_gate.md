# 投稿前文献更新门（2026-09-29 至 2026-10-02）

范围仅限四页短文的核心论证：评价污染审计、统一评价协议、角色化 Pilot 的保守定位。预算 0 CNY，不扩展 KG、RQ3 实验或系统综述。

## 必查项

1. 在 ACL Anthology、arXiv、Crossref/DOI 与公开引用索引中复跑保存的核心检索式，日期限定为 2026-08-26 之后。
2. 检查 `reference_tiers.csv` 中所有 `main-citable` 条目是否有勘误、撤稿或正式版本；重点复查 `tran2026equalbudget`。
3. 检查是否出现直接覆盖以下任一短文设定的工作：教育内容生成中的公平 SAS/MAS；中文二语 native/L1-culture conditioning；角色输出与评分字段错配/循环评价。
4. 对每个新命中保存题目、作者、年份、稳定 ID、原文/权威页、证据位置、纳排理由。未打开原文或权威页不得进入 approved。
5. 检查正文实际 citation keys 全部属于 approved，且正文论断不超过 `citation_claim_map.csv` 的允许强度。

## 门结果

- `no_material_change`：没有改变任务定位或引用边界的新证据；记录日期和检索结果后冻结。
- `metadata_update`：只修订版本、页码、DOI 或出版状态，不改变论证。
- `material_competitor_found`：发现直接竞争工作；立即收窄定位并交 P0/P4，不自行加入 first/absence/novelty 叙事。
- `source_unavailable`：保持 unverified，不引用。

完成截止：2026-10-02。负责人：P1；独立复核：P4；最终仲裁：P0/Human PI。
