# CulturaLink 跨文化中文学习平台 —— 模拟面试题与参考回答

> 以下问题模拟真实面试场景，按难度分层排列。每个问题后附参考回答，标注"诚实回答"（推荐）或"进阶回答"（加分项）。

---

## 一、方向性问题（暖场 / 考察项目真实性）

### Q1：请用两分钟介绍一下这个项目是做什么的。

**参考回答（诚实版）：**

这是一个面向留学生中文学习的智能平台。用户选择自己的母语（比如英语、日语、西班牙语）和 HSK 等级，系统会先以他的母语来解释中国文化概念，再把中国文化跟他母语文化做对比，最后生成场景化的练习题让他做。

核心技术是一个多智能体系统，5 个 AI Agent 分工协作：A1 负责分析学习者画像和焦虑度，A2 和 A3 并行生成母语阐释和文化对比，A4 生成练习题，A5 做质量审核。编排层用的是 LangGraph.js，支持根据缓存情况动态跳步。

**面试官想听到的：**
- 项目解决了什么问题（留学生学中文的文化隔阂感）
- 技术上有亮点（多 Agent 协作、LangGraph 图编排）
- 不背书，能用自己的话讲出来

---

### Q2：这个项目是你自己想的还是跟着教程做的？

**参考回答（诚实版）：**

需求是我自己想的。看到留学生朋友学中文时，很多教材都是从中文视角出发，没有真正从他们已有的文化背景出发做对比，学习起来特别费劲。所以我想做一个"以母语为锚点"的学习系统。

技术方案上，LangGraph 是我自己调研决定的——之前调研过 LangChain Agent，发现它的编排能力不够细粒度，LangGraph 的图结构更适合我需要的并行 + 条件分支场景。多智能体的设计参考了 OpenAI 的 multi-agent 论文，但具体实现是自己写的。

---

## 二、多智能体架构

### Q3：你的 5 个 Agent 之间怎么通信的？

**参考回答（诚实版）：**

通过 LangGraph 的共享状态 `LearningGraphState` 通信。状态是一个 TypeScript 对象，包含 anxiety_level、cultural_explanation、cross_cultural_comparison、learning_content 等字段。

每个 Agent 是一个函数，接收当前状态，返回更新后的状态。A1 的输出（焦虑度、焦虑描述）会作为 A2、A3 的输入，A2 和 A3 并行执行，通过 `addEdge` 汇入 A4。A4 和 A5 串行执行。

具体来说，A2 调 LLM 生成母语阐释，写入 `cultural_explanation` 字段；A3 调 LLM 生成跨文化对比，写入 `cross_cultural_comparison` 字段；A4 读取这两个字段，生成练习题，写入 `learning_content`。

**追问：如果不用 LangGraph，你打算怎么实现 A2 和 A3 的并行？**

用 Promise.all 手动控制。A2 和 A3 各自是一个 async 函数，`await Promise.all([agentA2(state), agentA3(state)])` 就能并行。串行的 A4、A5 就正常 await。但并行失败的处理（比如一个成功一个失败）、条件分支、图可视化就得自己写，比较麻烦。

**追问：为什么选 LangGraph 而不是自己写管道？**

自己写管道在需要并行的时候不好处理，更难支持条件分支。LangGraph 提供了图可视化、条件边、checkpoint 这些基础设施，未来要加 human-in-the-loop 或流式输出也方便。最重要的是——图结构本身就是一个清晰的架构文档，代码可读性比一堆 if-else 好很多。

---

### Q4：LangGraph 的 condition 边具体怎么写的？

**参考回答（诚实版）：**

```typescript
// 定义条件函数
const shouldSkipGeneration = (state: LearningGraphState) => {
  return state.from_cache ? "generateExercises" : "a1Profiler";
};

// 添加条件边：checkCache 节点根据 from_cache 决定下一步
graph.addEdge("checkCache", shouldSkipGeneration);

// 缓存命中：跳过 A1/A2/A3，直接生成练习题
// 未命中：正常走 A1 → (A2 || A3) → A4 → A5 → saveKB
```

条件函数返回的是目标节点名称的字符串，LangGraph 会根据返回值决定走哪条边。这比写一堆 if-else 清晰多了。

---

### Q5：你的 Agent 挂掉了怎么办？有错误恢复机制吗？

**参考回答（诚实版）：**

目前有重试机制，但比较简单。每个 Agent 的 LLM 调用外层包了 `withRetry`，失败后重试 3 次，间隔 1 秒。如果 3 次都失败，整个图执行就报错了。

更完善的方案应该是：给每个节点设置独立的超时时间（比如 A2/A3 30 秒、A4 90 秒），超时后走降级路径——比如命中缓存了就直接返回，没命中缓存就返回友好提示。目前还没做到这一步。

**追问：重试的时候参数会变吗？**

不会。`withRetry` 就是把同样的参数重新发给 LLM，没有退火、backoff 这些策略。这是目前的一个缺陷。

---

## 三、AI / LLM 集成

### Q6：一次完整的请求调几次 LLM？有没有想过怎么减少调用次数？

**参考回答（诚实版）：**

未命中缓存时 4 次（A2、A3、A4、A5 各 1 次），其中 A2 和 A3 是并行的，所以总等待时间是 max(A2, A3) + A4 + A5。命中缓存时只有 1 次（只调 A4 生成练习题）。

减少调用次数的思路：目前已经在做的——缓存命中的时候跳步。但还有可以优化的点：比如 A5 质量审核不一定每个题都完整审核，可以做抽样审核；或者 A4 生成练习题的时候，同时生成多个变体，后续随机选取，不一定每次都重新生成。

---

### Q7：你的 prompt 是怎么设计的？有做 prompt 工程吗？

**参考回答（诚实版）：**

每个 Agent 有自己的 system prompt，包含角色定义、输出格式要求、约束条件。A4 的 prompt 最复杂，需要同时参考 A2 的母语阐释和 A3 的文化对比，生成包含文化背景、语言点、练习题的内容。

prompt 工程我做了几件事：
- **格式约束**：要求 LLM 输出 JSON，后端用 `safeJsonParse` 容错解析（处理 markdown 代码块包裹、多余文字等情况）
- **few-shot 示例**：给 A5 的质量审核 Agent 提供了正反例，让它知道什么样的练习题算合格
- **语言指示**：A2/A3/A4 的 prompt 里明确指定了目标语言（比如"必须使用某种语言，禁止英语"）
- **长度控制**：prompt 里明确写了长度限制，防止 LLM 过度发挥

**追问：JSON 输出不稳定怎么办？**

目前用后端容错解析兜底。但更根本的方案是用 LLM 的 JSON Schema 约束输出格式，或者用工具调用（Tool Calling）强制结构化输出，不过当前用的模型这块能力不太稳定，所以 prompt 里还是用自然语言描述。

---

## 四、数据与知识工程

### Q8：Neo4j 和 Supabase 怎么选型的？为什么要两个数据库？

**参考回答（诚实版）：**

这是历史演进的结果。一开始只有 Supabase，存知识点阐释和对比数据。后来做知识图谱页面需要可视化展示，才引入了 Neo4j。

Neo4j 的优势是 Cypher 查询非常适合表达知识点之间的关系，做力导向图可视化很自然。但 Neo4j 的写入和复杂聚合查询不如 PostgreSQL 方便，所以最终是两者配合：Supabase 做 CRUD 和业务查询，Neo4j 做图关系查询和可视化。

**追问：Neo4j 实际用在哪些地方？**

目前主要是知识图谱可视化页面展示。但按照设计意图，它应该接入学习推荐——根据学习者的能力短板，从图谱里找相关的薄弱知识点推荐学习。不过这个功能还没实现，是目前系统的已知缺陷。

---

### Q9：你的知识点是怎么组织的？有没有做向量检索？

**参考回答（诚实版，直接说缺陷）：**

知识点目前结构化组织为 166 个语用任务（14 领域 / 56 场景），其中 22 个由人工精标、其余由 LLM 批量生成；每条含主题、HSK 等级、关联语言点、文化点等字段。检索目前靠 SQL 模糊匹配（向量检索待做）。

**没有做向量检索**，检索全靠 SQL 的 `ilike` 模糊匹配。比如用户选"日常社交"场景，系统会模糊匹配知识点标题里包含"日常"或"社交"的记录。匹配不到就兜底返回场景 ID 本身，不够精准；同义词也处理不了（比如"吃饭"搜不到"用餐"）。

理想上应该做向量检索：用 Embedding 模型把知识点描述转成向量，存到向量数据库里，用户输入自然语言时也转成向量，做余弦相似度检索。这部分在架构设计里写了，但还没实现，是已知缺陷。

---

### Q10：你的缓存策略是什么？缓存失效怎么处理的？

**参考回答（诚实版）：**

按 `knowledge_point_id + native_language` 查 Supabase 的 `cultural_explanations` 表，命中就跳过 A2 和 A3，只调 A4 生成练习题。缓存写入是在 A5 质量审核通过后，异步写入 Supabase。

**已知缺陷**：缓存不区分 HSK 等级。HSK 1 和 HSK 7 的同一个知识点"日常饮食"用的是同一份阐释，内容深度不匹配。理想上应该按三元组做缓存 key。

缓存失效目前没有处理——数据写进去就不变了。如果要支持内容更新，需要加版本号或者 TTL，但目前没有这个需求所以没做。

---

## 五、算法

### Q11：你的文化焦虑度公式怎么来的？有实验验证过吗？

**参考回答（诚实版，直接承认局限性）：**

这是我参考二语习得理论里的"文化适应假说"自己设计的启发式模型，没有做过用户实验验证。公式是：

```
焦虑度 = 0.4 × 文化差异感 + 0.3 × 母语干扰比 + 0.2 × 形式陌生度 + 0.1 × 新奇度
```

权重 0.4/0.3/0.2/0.1 是拍脑袋分配的，意思是"文化差异感"对焦虑度影响最大。因子是从知识点数据里提取的关键词频率计算的，不是真实测量。

**追问：那这个公式有没有实际用过？**

用过。A1 计算出的焦虑度会作为 prompt 的一部分传给 A2 和 A3，告诉 LLM"这个学习者的焦虑度较高，请用更温和的方式表达文化差异"。但实际效果怎么样，我没有做 A/B 测试。

---

### Q12：贝叶斯知识追踪（BKT）具体怎么实现的？

**参考回答（诚实版）：**

每轮答题后，根据答题结果更新学习者对各维度的掌握概率：

```
P(掌握|答题) = [P(掌握) × P(答对|掌握)] / P(答对)
```

初始 P(掌握) = 0.5，P(答对|掌握) = 0.9，P(答对|未掌握) = 0.2。每次答对就往 1 的方向更新，答错就往 0 的方向更新。

更新后的能力向量 `[语法, 听力, 口语, 阅读, 文化语用]` 会写回 Supabase 的 `learners` 表。

**追问：这个能力向量在实际学习流程里怎么用的？**

目前只是存着，实际没用起来。理论上应该根据能力向量的短板来推荐学习内容，但推荐算法还没接入主流程，是半成品。

---

## 六、工程与架构

### Q13：前端状态管理怎么做的？为什么不用 Redux 或 Zustand？

**参考回答（诚实版）：**

只有一个全局状态：当前学习者信息（ID、母语、HSK 等级、焦虑度、能力向量）。用 React Context + `useState` 实现了一个轻量级 `LearnerContext`，没有引入 Redux 或 Zustand。

原因是状态复杂度极低，只有一个对象需要跨页面共享，不需要 slices、reducers、middleware 这套东西。Context 的 Provider 挂在 layout.tsx 最外层，首页和 learning 页都通过 `useLearner()` 获取和更新状态。

---

### Q14：你怎么保证返回给前端的 JSON 格式是稳定的？

**参考回答（诚实版）：**

LLM 返回的不是标准 JSON（可能有 markdown 代码块包裹、多余文字），所以后端用 `safeJsonParse` 做容错解析：

```typescript
function safeJsonParse(text: string): any {
  // 1. 直接尝试 JSON.parse
  // 2. 失败则提取 markdown 代码块内的内容
  // 3. 失败则提取首尾花括号内的内容
  // 4. 失败则报错
}
```

此外，Agent 的 prompt 里明确指定了 JSON Schema 要求输出包含必填字段，但 Schema 是自然语言描述的，不是强制约束，所以偶尔还是会出现字段缺失。

**追问：Schema 怎么变成强制约束？**

用 LLM 的 JSON Schema 输出功能——在 API 调用时传入 `response_format: { type: "json_schema", ... }`，强制 LLM 输出的 JSON 必须符合 Schema。但这取决于模型是否支持，当前用的模型这块能力不太稳定，所以 prompt 里还是用自然语言描述。

---

### Q15：这个项目最大的技术难点是什么？你怎么解决的？

**参考回答（诚实版，有反思）：**

最大的难点是多 Agent 之间的状态传递和格式校验。

5 个 Agent 串并行协作，每个 Agent 输出结构化 JSON 给下一个用。最开始直接让 Agent 输出 JSON，但 LLM 返回格式很不稳定，经常出现字段缺失、类型错误、markdown 包裹等问题。

解决方案是分两层：
1. **Agent 层**：prompt 里写清楚 JSON Schema 自然语言描述，加上 few-shot 示例
2. **编排层**：每个 Agent 输出后用 `safeJsonParse` 容错解析，解析失败就重试（最多 3 次），3 次都失败就报错退出

这个方案不完美，但能在复杂度和可靠性之间取得平衡。未来理想的做法是用工具调用强制结构化输出。

---

## 七、诚实问题（考察 self-awareness）

### Q16：这个项目有哪些地方你觉得做得不好，想改进的？

**参考回答（按优先级排序，诚实但有思考）：**

1. **向量检索缺失（最高优先级）**：目前检索全靠 SQL 模糊匹配，不够精准。应该上向量相似度检索。
2. **BKT 未形成闭环**：贝叶斯追踪计算了，但计算结果没有用来驱动推荐算法。
3. **Neo4j 未接入主流程**：图数据库目前只用于可视化页面展示，没有接入学习推荐逻辑。
4. **偏见检测是规则匹配**：理想上应该加语义检测模型。
5. **缓存不区分 HSK 等级**：同一知识点不同难度用同一份阐释，内容深度不匹配。

---

### Q17：如果让你重新做这个项目，你会做什么不同的选择？

**参考回答（诚实 + 有洞察）：**

1. **先做向量检索，再做 Agent**：现在回头看，多 Agent 编排虽然复杂，但逻辑是清晰的。真正影响用户体验的是"找到合适的学习内容"这个环节——SQL 模糊匹配太弱了。
2. **Schema 约束用工具调用而不是 prompt**：prompt 里的 JSON Schema 是软约束，应该直接用模型的工具调用能力强制结构化输出，parse 失败的概率会大幅降低。
3. **先接一个模型调好了，再扩展多 Agent**：目前每轮学习要 4 次 API 调用，成本和延迟都比较高。如果先只做一个 Agent 把体验做顺，再拆成多 Agent，会减少很多调试成本。
4. **用 PostgreSQL 的 pgvector 而不是单独维护 Neo4j**：现在两个数据库增加了运维复杂度。如果需求只是"知识点 + 关系 + 可视化"，PostgreSQL + pgvector + D3.js 完全能覆盖，不用引入 Neo4j。

---

### Q18：这个项目有做测试吗？

**参考回答（诚实）：**

没有做自动化测试。主要靠 curl 手动验证接口，加上生产环境日志排查问题。

这是个缺陷。理想上应该有：API 的单元测试（验证 results API 的焦虑度计算公式）、组件的快照测试（验证学习页渲染不崩溃）、集成测试（验证完整学习链路）。目前只有 TypeScript 编译检查和 ESLint linting。

---

## 八、反问面试官（最后环节）

面到最后，可以反问面试官：

- "你们团队的多 Agent 系统是怎么做错误处理的？有什么最佳实践吗？"（展现你对团队协作的兴趣）
- "这个岗位的 LLM 集成部分具体用的是什么方案？API 调用还是本地部署？"（了解实际技术栈）
- "你们怎么做 AI 输出的质量评估？有人工审核流程吗？"（了解质量保障体系）
- "你们的知识点数据是怎么积累的？有专人维护吗？"（了解知识工程流程）
